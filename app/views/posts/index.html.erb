<h1>Cowboy Club</h1>

<ul>
  <% @posts.each do | post | %>
    <li>
      <h2>post.title</h2>
      <p>post.body</p>
      <% if user_signed_in? %>
        <p>post.user.username</p>
      <% end %>
    </li>
  <% end %>
</ul>